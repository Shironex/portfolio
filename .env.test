# Test Environment Variables
# These are mock values for testing purposes only

# Node Environment
NODE_ENV=test

# Redis (will use ioredis-mock)
REDIS_HOST=redis://localhost:6379

# Resend (mocked in tests)
RESEND_API_KEY=test_resend_api_key_1234567890
RESEND_MAIL_TO=test@example.com

# Cloudflare Turnstile (mocked in tests)
TURNSTILE_SECRET_KEY=test_turnstile_secret_key
NEXT_PUBLIC_TURNSTILE_SITE_KEY=test_turnstile_site_key

# Discord Webhook (mocked in tests)
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/test/test_webhook_url

# Analytics (optional for tests)
ANALYTIC_URL=https://analytics.example.com
ANALYTIC_ID=test_analytics_id

# Public URL
NEXT_PUBLIC_PUBLIC_URL=http://localhost:3000

# Sentry (disabled in tests, mocked)
SENTRY_DSN=
SENTRY_ORG=
SENTRY_PROJECT=

# CI Flag (optional)
CI=false
